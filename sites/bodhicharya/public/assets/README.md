# Assets

Drop real brand assets here once available: logo, hero/photo imagery, team photos, the Nepal
Companies Act registration certificate (PDF), favicon replacement, etc.

Until then, the site uses simple CSS placeholders (gradient brand mark, inline SVG icons) so it
renders correctly without any binary assets checked in.

Suggested naming, once you add files (update the referencing HTML/CSS accordingly):

- `logo.svg` / `logo.png` — replaces `.brand-mark` in `css/style.css` and `favicon.svg`
- `hero.jpg` — optional hero background/image on `index.html`
- `healthcare.jpg`, `education.jpg`, `spiritual.jpg` — optional photos for the "What we do" /
  Programs cards, replacing the inline SVG icons
- `registration-certificate.pdf` — optional, linked from the About/Registration section
